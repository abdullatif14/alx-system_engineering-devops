#!/usr/bin/env bash
# Use the ps command with specific options to display the process list
# -ef: Show all processes, in full format
# | grep bash: Filter the output to only show lines containing the word "bash"
# | awk '{print $2}': Extract the second column (the process ID)

ps -ef | grep bash | awk '{print $2}'
